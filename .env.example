# ===================================
# 環境変数設定ファイル (.env.example)
# ===================================
# このファイルをコピーして .env または .env.local として使用してください。
# 本番環境では Cloud Run の環境変数として設定してください。

# -----------------------------------
# 必須: データベース接続 (PostgreSQL / Cloud SQL)
# -----------------------------------
DATABASE_URL=postgresql://[user]:[password]@[host]:[port]/[database]
# Cloud SQL接続の場合: postgresql://user:password@/dbname?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME

# -----------------------------------
# 任意: Supabase接続 (使用する場合)
# -----------------------------------
# NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# -----------------------------------
# 任意: Google Cloud Platform
# -----------------------------------
# GCP_PROJECT_ID=your-gcp-project-id
# GCP_KEY_FILE=/path/to/service-account-key.json
# GCS_BUCKET_NAME=railway-maintenance-storage

# -----------------------------------
# 任意: ストレージ設定
# -----------------------------------
# STORAGE_TYPE=gcs  # gcs または local

# -----------------------------------
# アプリケーション設定
# -----------------------------------
NODE_ENV=production
PORT=8080

# -----------------------------------
# CORS設定 (本番環境用)
# -----------------------------------
# ALLOWED_ORIGINS=https://your-domain.run.app

# -----------------------------------
# API設定
# -----------------------------------
# NEXT_PUBLIC_API_URL=/api
