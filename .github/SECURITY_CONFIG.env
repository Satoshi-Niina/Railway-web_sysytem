# 📦 セキュリティアラート設定

# このファイルは、プロジェクトのセキュリティと依存関係の監視設定を管理します

## 🔔 通知先設定

# GitHub通知
# デフォルトでは、リポジトリ管理者に通知されます
# 追加の通知先を設定する場合は、以下を調整してください

NOTIFY_ON_SECURITY_ALERT=true
NOTIFY_ON_DEPENDENCY_UPDATE=true
NOTIFY_ON_NODE_VERSION=true

## 📧 Slack通知（オプション）

# Slack Webhook URLを GitHub Secrets の SLACK_WEBHOOK_URL に設定してください
# 設定手順:
# 1. Slack App を作成: https://api.slack.com/apps
# 2. Incoming Webhooks を有効化
# 3. Webhook URL をコピー
# 4. GitHub リポジトリの Settings → Secrets → Actions で SLACK_WEBHOOK_URL として追加

# 通知するセキュリティレベル（これらは自動的に監視されます）
NOTIFY_CRITICAL=true   # 緊急（即座に通知）
NOTIFY_HIGH=true       # 高（即座に通知）
NOTIFY_MODERATE=false  # 中（週次レポート）
NOTIFY_LOW=false       # 低（週次レポート）

## 🤖 自動修正の設定

# 自動的に修正されるセキュリティレベル
AUTO_FIX_CRITICAL=true  # Criticalは自動修正
AUTO_FIX_HIGH=true      # Highも自動修正
AUTO_FIX_MODERATE=false # Moderateは手動確認

# 自動的に更新されるバージョン範囲
AUTO_UPDATE_PATCH=true   # パッチバージョン（1.0.0 → 1.0.1）
AUTO_UPDATE_MINOR=false  # マイナーバージョン（1.0.0 → 1.1.0）
AUTO_UPDATE_MAJOR=false  # メジャーバージョン（1.0.0 → 2.0.0）

## ⏰ スケジュール設定

# GitHub Actionsの実行スケジュールは以下のファイルで管理されています:
# - .github/workflows/dependency-check.yml (毎週日曜 3:00)
# - .github/workflows/auto-update-dependencies.yml (毎週月曜 2:00)
# - .github/workflows/node-version-check.yml (毎月1日 3:00)

# スケジュールを変更する場合は、該当YAMLファイルのcron設定を編集してください
# cron形式: '分 時 日 月 曜日' (UTC時間)
# 例: '0 18 * * 0' = 日曜日 18:00 UTC = 月曜日 03:00 JST

## 🎯 監視対象

# 以下のディレクトリのpackage.jsonを監視
MONITOR_ROOT=true
MONITOR_CLIENT=true
MONITOR_SERVER=true

# 特定のパッケージを監視対象から除外（必要な場合）
# EXCLUDE_PACKAGES=package1,package2

## 📊 レポート設定

# 週次レポートにセキュリティ監査結果を含める
INCLUDE_AUDIT_IN_REPORT=true

# 週次レポートに更新可能なパッケージ一覧を含める
INCLUDE_UPDATES_IN_REPORT=true

# レポートの詳細度（summary / detailed）
REPORT_DETAIL_LEVEL=detailed

## 🔐 セキュリティポリシー

# 脆弱性の対応期限（日数）
CRITICAL_RESPONSE_DAYS=1   # Criticalは1日以内
HIGH_RESPONSE_DAYS=7       # Highは1週間以内
MODERATE_RESPONSE_DAYS=30  # Moderateは1ヶ月以内

# 古いパッケージの警告しきい値（日数）
OUTDATED_WARNING_DAYS=90   # 90日以上更新がない場合に警告

## 📝 メモ

# このファイルは設定の参考として使用されます
# 実際の設定は以下のファイルで行われます:
# - .github/workflows/*.yml (GitHub Actions)
# - .github/dependabot.yml (Dependabot)
# - renovate.json (Renovate Bot)
#
# 環境変数として使用する場合は、GitHub Secrets に追加してください
