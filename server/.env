# ===================================
# ローカル開発用環境変数 (.env.development)
# ===================================
# このファイルはローカル開発環境（Next.js & Node.js）で使用されます。

# 環境設定
NODE_ENV=development

# プロジェクト共通設定
PROJECT_NAME=railway-maintenance-system
PROJECT_VERSION=2.0.0

# アプリケーションURL
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api
NEXT_PUBLIC_SERVER_URL=http://localhost:3001

# サーバー設定 (Express API)
PORT=3001
HOST=localhost

# -----------------------------------
# データベース設定（PostgreSQL 17 / webappdb）
# -----------------------------------

# ローカルDB接続文字列
# フォーマット: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://postgres:Takabeni@localhost:5432/webappdb

# 個別設定項目（ライブラリやスクリプトで使用）
DB_HOST=localhost
DB_PORT=5432
DB_NAME=webappdb
DB_USER=postgres
DB_PASSWORD=Takabeni
DB_SSL=false

# 接続プール設定
DB_POOL_MIN=2
DB_POOL_MAX=10

# -----------------------------------
# アプリケーション設定
# -----------------------------------

# CORS設定
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_CREDENTIALS=true

# ストレージ設定（Google Cloud Storage）
STORAGE_TYPE=gcs
GCP_PROJECT_ID=maint-vehicle-management
GCS_BUCKET_NAME=railway-maintenance-storage
GCP_KEY_FILE=./gcp-service-account-key.json

# ログ設定
LOG_LEVEL=debug
NEXT_PUBLIC_LOG_LEVEL=debug

# セッションセキュリティ
SESSION_SECRET=railway_maintenance_local_secret_2026
SESSION_TIMEOUT=3600000  # 1時間
ENABLE_HELMET=false

# -----------------------------------
# 使用停止参考情報 (Cloud SQL 接続例)
# -----------------------------------
# Cloud SQL Auth Proxy等を使用する場合の例:
# CLOUD_DB_URL=postgresql://postgres:Takabeni@/webappdb?host=/cloudsql/maint-vehicle-management:asia-northeast2:free-trial-first-project&options=-c%20search_path=public
